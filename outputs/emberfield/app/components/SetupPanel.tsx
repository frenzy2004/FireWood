"use client";

import { X } from "@phosphor-icons/react";
import { useState } from "react";
import type { SavedAsset } from "../hooks/use-dashboard";

const categories = ["field", "orchard", "barn", "livestock", "workforce", "storage", "other"];
export function SetupPanel({ onClose, onSaved }: { onClose: () => void; onSaved: (asset: SavedAsset) => void }) {
  const [name, setName] = useState(""); const [address, setAddress] = useState(""); const [latitude, setLatitude] = useState(""); const [longitude, setLongitude] = useState(""); const [category, setCategory] = useState("field"); const [miles, setMiles] = useState("25"); const [notes, setNotes] = useState(""); const [error, setError] = useState(""); const [saving, setSaving] = useState(false);
  const lookup = async () => { try { const response = await fetch(`/api/geocode?address=${encodeURIComponent(address)}`); const data = await response.json() as { result?: { location?: { lat: number; lon: number } }; error?: string }; if (!response.ok || !data.result?.location) throw new Error(data.error ?? "Address lookup did not return a coordinate."); setLatitude(String(data.result.location.lat)); setLongitude(String(data.result.location.lon)); setError(""); } catch (cause) { setError(cause instanceof Error ? cause.message : "Address lookup is unavailable. Enter coordinates directly."); } };
  const save = async (event: React.FormEvent) => { event.preventDefault(); setSaving(true); setError(""); try { const response = await fetch("/api/assets", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ name, category, latitude: Number(latitude), longitude: Number(longitude), radiusKm: Number(miles) * 1.60934, notes: notes || undefined }) }); const data = await response.json().catch(() => ({})) as { asset?: SavedAsset; error?: string }; if (!response.ok || !data.asset) throw new Error(data.error ?? "Asset could not be saved."); onSaved(data.asset); } catch (cause) { setError(cause instanceof Error ? cause.message : "Asset could not be saved. Your entries are still here to retry."); } finally { setSaving(false); } };
  return <div className="setup-backdrop" role="dialog" aria-modal="true" aria-labelledby="setup-title"><form className="setup-panel panel" onSubmit={save}><div className="panel-heading"><div><p className="eyebrow">Asset setup</p><h2 id="setup-title">Add a farm asset</h2></div><button type="button" className="icon-button" onClick={onClose} aria-label="Close asset setup"><X size={19} /></button></div><label>Name<input required value={name} onChange={(event) => setName(event.target.value)} /></label><label>Address lookup<div className="lookup-row"><input value={address} onChange={(event) => setAddress(event.target.value)} placeholder="Optional rural address" /><button type="button" onClick={() => void lookup()}>Lookup</button></div></label><div className="form-grid"><label>Latitude<input required type="number" step="any" value={latitude} onChange={(event) => setLatitude(event.target.value)} /></label><label>Longitude<input required type="number" step="any" value={longitude} onChange={(event) => setLongitude(event.target.value)} /></label></div><div className="form-grid"><label>Category<select value={category} onChange={(event) => setCategory(event.target.value)}>{categories.map((item) => <option key={item}>{item}</option>)}</select></label><label>Radius miles<input required type="number" min="0.62" max="100" step="0.1" value={miles} onChange={(event) => setMiles(event.target.value)} /></label></div><label>Notes<textarea value={notes} onChange={(event) => setNotes(event.target.value)} /></label>{error && <p className="form-error" role="alert">{error}</p>}<button className="primary-button" disabled={saving}>{saving ? "Saving" : "Save asset"}</button></form></div>;
}
